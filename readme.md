# Dokka test project

## Generate dokka
Run `./gradlew dokkaHtml` task

## Test data
### dokka-test-plugin
A test plugin for dokka located in the included build.

## Generale way to use
Generate the Dokka output for Dokka version X and X-1 where the X is a release to test and compare outputs. 

### Version bump
In the gradle version catalog.